# Redis 部署环境变量
# 复制为 .env 后按需修改：cp .env.example .env

# 可选：端口映射到主机（默认 6379）
# REDIS_PORT=6379

# 可选：设置密码（推荐生产环境）。设置后中间层 REDIS_URL 需带密码，如 redis://:password@host:6379
# REDIS_PASSWORD=your_secure_password

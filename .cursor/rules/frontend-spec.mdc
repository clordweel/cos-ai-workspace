---
description: 前端 UI 布局、组件职责与约定（基于 docs/FRONTEND_SPEC.md）
globs: frontend/**/*
alwaysApply: false
---

# 前端规范要点

## 技术栈与主题
- **Vue 3** + **Nuxt 3** + **Tailwind** + **Shadcn-vue**。当前为**浅色**实现：`bg-zinc-50`、`border-zinc-200`、白底卡片；可扩展深色。
- 样式：圆角容器（`rounded-xl` / `rounded-2xl`）、统一边框与阴影，避免传统密集表格。

## 布局（workspace）
- **布局文件**：`layouts/workspace.vue`。全屏 `h-screen`、主体横向 flex、`p-3`。
- **Workspace UI 状态（单一数据源）**：`useWorkspaceLayout()` 根据「会话列表 + 会话聊天(由 route) + 应用标签栏折叠/展开 + 应用内容展开/折叠」推导 **layoutMode**，并用 **grid 左栏/右栏** 分配空间：**gridTemplateColumns**、**appPanelMaxWidthCss**。断点从窄到宽（sm / lg / xl）决定左栏为 1fr 或固定 288px，右栏为 0fr 或 minmax(0, 1000px)。不依赖 ResizeObserver。
- **四种典型布局模式**：`list_app_content_expanded`（默认）| `list_chat_app_content_expanded` | `list_chat_app_tabs_content_collapsed` | `list_chat_app_sidebar_content_collapsed`。应用区最大宽度常量 `WORKSPACE_APP_PANEL_MAX_WIDTH_PX`（如 1000px）。
- **左栏（会话区）**：`<slot />`（一般为 `pages/space/[[id]].vue`）。宽度由 grid 列宽决定：应用区关闭时 1fr；应用区打开时 &lt; lg 为 1fr，lg～xl 为 288px(w-72)，xl+ 为 1fr。
- **右栏（应用区）**：`section`（`rounded-2xl`、白底、`max-width` 由 `useWorkspaceLayout` 提供）。内为 **WorkspaceAppNav** + 应用内容区；由 `useAppView()` 的 `isPanelOpen`、`isContentVisible`、`appSidebarExpanded` 等控制，过渡动画 `app-panel`。

## 应用区结构
- **WorkspaceAppNav**：侧栏为**浏览器标签式**，宽度 `w-48`/`w-12` 可折叠，宽度过渡 200ms。悬停 500ms 才展开，移出 180ms 延迟收起；可固定侧栏（顶部工具栏按钮）。自上而下：**已打开标签列表**（图标 + 标题 + 悬停显示关闭按钮，点击切换、× 关闭）→ 底部固定「新标签」「设置」按钮。
- **应用内容区**：内层 `rounded-xl`、`bg-zinc-50/50`、内阴影+外阴影。**AppPanel** 按当前选中标签的 `currentView` 切换：`home` | `contacts` | `bots` | `settings` | `auth` | `app`。

## 会话区（space 页）
- **useAppView**：标签 `tabs`、`activeTabId`、`activeTab`、`addTab`、`closeTab`、`switchTab`；面板/内容 `isPanelOpen`、`isContentVisible`、`currentView`、`openPanel`、`openNavPage`、`toggleContentPanel`；侧栏 `isSidebarPinned`、`isSidebarHovered`、`scheduleSidebarExpand`、`scheduleSidebarLeave`、`cancelSidebarLeave`、`toggleSidebarPinned`。
- Layout provide **`isSessionExpanded`**（应用区关闭或内容区折叠时为 true）。为 true 时：会话列表 `w-64` 与聊天区左右并排；否则列表在上或仅聊天，顶栏可带「返回」。
- **SessionListHeader**：Logo + 标题、新会话、搜索。会话列表当前项高亮（emerald）。主区：有 `chatId` 时顶栏 + 消息流（**ChatMessageBubble**）+ 输入框；无 `chatId` 时居中提示 + 新会话按钮。

## UX 与 API
- 主界面以**对话流**为主，结构化结果用**任务卡片**（订单进度、库存摘要、BOM 状态、待确认物料等）。
- 对话：`POST /api/chat/stream`，消费 **SSE**，打字机展示。确认创建物料：卡片「确认」→ `POST /api/material/confirm`（`draft_id`、用户标识）。

## Shadcn-vue 与 UI 组件
- **安装方式**：仅通过官方 CLI 安装，禁止手动复制或手写 `components/ui` 下组件。命令：`pnpm dlx shadcn-vue@latest add <组件名>`（在 `frontend` 目录执行）；多组件可连续 `add checkbox dropdown-menu select`。
- **结构**：遵循 [Nuxt 安装文档](https://www.shadcn-vue.com/docs/installation/nuxt)：Tailwind 使用 `@tailwindcss/vite` + `~/assets/css/tailwind.css`，`nuxt.config` 中 `modules: ['shadcn-nuxt']`、`css` 与 `vite.plugins` 按文档配置；UI 组件目录为 `@/components/ui`，由 `components.json` 与 CLI 维护。
- **Vue 内使用 @apply**：若在 SFC 的 `<style>` 中使用 `@apply`，须在样式块顶部加 `@reference "~/assets/css/tailwind.css";`（Tailwind v4 要求）。

## 关键文件
- 布局/页：`layouts/workspace.vue`、`pages/space/[[id]].vue`
- 组件：`SessionListHeader.vue`、`WorkspaceAppNav.vue`、`AppPanel.vue`、`ChatMessageBubble.vue`、`TaskCard/*`
- 状态：`composables/useWorkspaceLayout.ts`（布局模式与空间分配）、`useAppView.ts`、`useChatSessions.ts`、`useChatStream.ts`

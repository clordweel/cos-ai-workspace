---
description: 开发时进行产品化考量，不追求最低限度实现
alwaysApply: true
---

# 产品化考量

开发时**不得仅以最低限度实现**，需兼顾可交付产品的质量与体验。

## 原则

- **功能完整**：覆盖主流程与常见边界（空态、错误态、加载态、权限不足等），不留下「先能跑」的半成品。
- **体验一致**：交互、文案、样式与现有规范一致；新组件/页面符合 `.cursor/rules/frontend-spec.mdc` 与项目 UX 约定。
- **可维护与可观测**：关键路径有清晰错误处理与日志；对外接口有合理校验与错误信息，便于排查与迭代。
- **适度前瞻**：在合理范围内考虑扩展与复用（如参数化、可配置），避免为「只做这一次」写死逻辑，但不过度设计。

## 实践示例

- 列表/表格：空态提示、加载骨架或 loading、错误重试或提示，而非仅「有数据时展示」。
- 表单/操作：校验、提交中状态、成功/失败反馈，而非仅「调通接口」。
- 新 API/路由：参数校验、错误码与错误信息、日志；必要时文档或注释说明用途与约束。

当需求描述偏简略时，主动补全上述产品化维度并在实现中体现，而不是只做「刚好满足字面需求」的最小实现。

---
description: Agent 工作流与上下文最佳实践（2026 推荐）
alwaysApply: true
---

# Agent 工作流与上下文

## Plan 优先（生产级改动必用）
- **大功能、多文件、易返工时**：先用 **Plan Mode**（Shift+Tab），让 Agent 先调研代码库、澄清需求并产出实现计划，再编码。可把计划保存到 `.cursor/plans/` 供团队复用。
- **小修小改**（单文件、明确需求）可跳过 Plan，直接改。

## 动态上下文（减少 token、提高准确度）
- **优先按需拉取**：用语义搜索、grep、@ 引用具体文件/行，避免一次性贴入大段代码或整仓描述。
- **新需求先看文档**：先读 `docs/PROJECT_STATUS.md`、`docs/ARCHITECTURE.md` 再动手；细节用 @ 指向相关文件。
- **规则即静态上下文**：团队约定写在 `.cursor/rules/`，Agent 会自动加载；不必在对话里重复长篇说明。

## 会话长度与新会话
- **对话轮次较多（约 20+）或开新功能时**：建议**新开会话**，用「基于某 commit/PR 继续」或 @ 文件接棒，避免上下文膨胀与遗忘。
- **单次会话 scope 尽量单一**：例如只改 `apps/web` 或只改 `apps/api`。

## 与项目规则的配合
- 上述与 `project-global.mdc` 中「会话与上下文」一致；本规则侧重 Plan、动态上下文与断点续传。

---
description: 本工作区 AI 交互工作台的关键全局规则与约定
alwaysApply: true
---

# 项目全局规则（供其它会话复用）

## 语言与输出
- **所有对话与产出统一使用中文。**

## 项目定位与技术栈
- **前端**：Vue 3 + Nuxt 3 + Tailwind + Shadcn-vue，对话流 + 任务卡片，极简「去 ERP 化」UX。
- **中间层**：Node.js (Fastify)，SSE 流式代理、Dify/ERP 编排、鉴权；Dify API Key 仅放中间层，前端不直连 Dify/Frappe。
- **后端**：ERPNext v16 + cos App（REST API）；前端不直连 Frappe，经中间层访问。

## 流式与 AI 行为
- **必须支持高性能 SSE 流式输出**，打字机效果；禁止一次性拉取长文本造成卡顿。
- **AI 思考过程**：前端需完整展示并在思考结束后保留；中间层在流结束前发送 `thinking` 事件（含 `fullText`）；前端监听 `onThinkingDone` 更新并保留内容。解析 `<think>` 时需兼容大小写、可选空格及 HTML 实体（如 `&lt;think&gt;`）。
- **Dify 事件**：中间层转发 `agent_thought`、`tool_call` 等为 `dify_event`；前端根据其展示「正在调用工具」等状态。`useChatStream.ts` 处理流时**不得重置** `lastEvent`。

## 安全与写入
- 写入路径：**意图 → 参数/草稿 → 用户确认 → 权限校验 → 再写入**。物料创建走「草稿 → 确认 → create_from_draft」。
- **`.env` 文件**：需要更新时**直接执行**，无需向用户确认后再改。
- 中间层负责 CORS 及 `Access-Control-Allow-*` 等头部，确保前后端通信无跨域错误。

## 认证与用户配置
- **认证与用户信息**：统一由 **Logto** 处理。前端通过 Logto 登录，中间层在回调中写 Cookie；用户姓名、邮箱、头像等来自 Logto，经 `GET /api/auth/me` 返回。
- **用户偏好**：主题、字体档位、通知开关等存 **Logto 自定义数据（customData）**，经 Management API 读写；前端 `useUserPreferences()` 登录后从 `/api/auth/me` 的 `preferences` 加载，修改经 `PATCH /api/auth/me/preferences` 写回；未登录时使用本地 localStorage / color-mode。
- **Matrix**：仅作会话/聊天后端，用户身份以 Logto 为准；Logto 登录成功后中间层将用户同步到 Synapse（创建/更新），所需配置见文档。**勿在 customData 中存敏感信息**（JWT 为 base64、易被截获）。
- 详见 **`docs/AUTH_AND_USER_CONFIG.md`**；Matrix 同步与密码流程见 **`docs/LOGTO_MATRIX_AUTH_FLOW.md`**。
- **Matrix 参考**：涉及 Matrix 协议、会话、消息、房间等实现时，**允许并鼓励**从 Element、Cinny、FluffyChat、SchildiChat 等 Matrix 开源客户端中搜索、参考实现方案（可用 web_search、mcp_web_fetch 或 Context7 等）。

## 前端约定
- 布局与组件详见 **`.cursor/rules/frontend-spec.mdc`**（在编辑 `frontend/**` 时生效）。主布局为 `layouts/workspace.vue`（会话区 + 应用区），会话页为 `pages/space/[[id]].vue`。
- 状态：`useChatSessions.ts` 管理全局会话与消息；`useAppView.ts` 管理应用区（`isPanelOpen`、`isContentVisible`、`currentView`：`home` | `contacts` | `bots` | `settings` | `auth` | `app`）。Layout 向子组件 provide `isSessionExpanded`。认证唯一入口为 Logto；用户偏好用 `useUserPreferences()`，与 Logto customData 同步。
- 样式：统一浅色主题，圆角容器（如 `rounded-xl`、`border-zinc-200`、`bg-white/80`）。

## 会话与上下文
- **单次会话 scope 尽量单一**：例如只改 `frontend` 或只改 `middleware`，避免同一对话里跨多模块、多需求。
- **新需求先看文档再动手**：先看 `docs/PROJECT_STATUS.md` 与 `docs/ARCHITECTURE.md`，再改代码；细节用 @ 引用具体文件或行号，减少无关上下文。
- **大改动拆子任务**：每个子任务单独会话完成，用 git 提交 / PR 串起来；长会话适时收尾，新会话用「基于某 commit/PR 继续」接棒。

## 代码结构（便于 Agent 导航）
- **单文件尽量控制在约 300 行内**：过长时按职责拆成多个模块或 composable，避免单文件堆积过多逻辑。
- **文件名语义化**：如 `password-hashing.ts`、`sessionStore.ts` 等比泛用 `helpers.ts` 更利于语义搜索与按需加载。
- **相关文件就近放置**：同领域逻辑放在同一目录（如 `middleware/src/services/auth/`），减少跨目录遍历。

## 仓库与文档
- 默认分支：`develop`。重要变更记录在 `logs/CHANGELOG.md`；多维度状态见 `docs/PROJECT_STATUS.md`；架构与 API 见 `docs/`（ARCHITECTURE、STREAM_AND_SAFETY、FRONTEND_SPEC、API_SPEC、AUTH_AND_USER_CONFIG、LOGTO_MATRIX_AUTH_FLOW、CONFIG_AND_AUTH_RESEARCH）。
- **提交信息**：遵循 **`.cursor/rules/git-commit.mdc`**（`type(scope): 描述`，scope 用实际目录如 `frontend`、`middleware`）。

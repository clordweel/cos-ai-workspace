---
description: 任务执行时先提交、完成后给检验方式与效果说明
alwaysApply: true
---

# 任务执行与提交

## 收到「下一步任务」指令时

当用户**明确给出下一步任务指令**（如「下一步任务」「执行下一步」「继续下一阶段」等）时：

1. **先提交当前更改**：若有未提交的变更，先执行 `git add` 与 `git commit`，提交信息遵循 `.cursor/rules/git-commit.mdc`（type(scope): 描述）。
2. **再执行下一步任务**：在干净工作区上开始用户指定的下一阶段/步骤，避免未提交改动与下一步混在一起。

## 每步任务完成时

每完成**一个任务或子步骤**后，尽量给出：

1. **检验方式**：如何验证该步已正确完成（例如：运行哪条命令、访问哪个 URL、检查哪份文件或输出、回归清单中的哪几项）。
2. **效果说明**：该步做完后的具体效果或状态（例如：新增了哪些能力、哪些脚本/路由可用、与参考实现的对应关系）。

便于用户自测与后续介入修复；多步骤任务可在全部完成后做一次汇总的检验与效果说明。

# 标准 monorepo 结构（参考 Element 等）：apps/* + packages/*；现有 frontend、middleware 保持不变
packages:
  - frontend
  - middleware
  - "apps/*"
  - "packages/*"

onlyBuiltDependencies:
  - '@parcel/watcher'
  - esbuild
  - msw
  - sharp
  - vue-demi

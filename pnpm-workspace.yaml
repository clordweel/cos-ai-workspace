# 标准 monorepo 结构（参考 Element 等）：apps/* + packages/*；frontend、middleware 已移除
packages:
  - "apps/*"
  - "packages/*"

onlyBuiltDependencies:
  - '@parcel/watcher'
  - esbuild
  - msw
  - sharp
  - vue-demi

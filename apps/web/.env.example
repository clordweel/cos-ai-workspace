# ============ apps/web 环境变量示例 ============
# 复制为 .env 后按实际修改。仅 dev 时生效（webpack.config.js 读取）。

# 开发时 /api 代理目标（API 服务地址），本机 10.1.1.21 时 API 通常为 3000 端口
WEBPACK_PROXY_TARGET=http://10.1.1.21:3000
# 同机开发也可用：
# WEBPACK_PROXY_TARGET=http://localhost:3000
